<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:background="@color/calendar_content_background"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <LinearLayout
        android:gravity="center"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="@dimen/refresh_bar_height">
        <ProgressBar
            android:id="@+id/progressbar"
            android:layout_width="@dimen/refresh_progressbar_height"
            android:layout_height="@dimen/refresh_progressbar_height"
            android:indeterminateOnly="true"
            android:layout_marginEnd="4dp"/>
        <TextView
            android:textAppearance="@style/TextAppearance.AppCompat.Small"
            android:textColor="@color/actionbar_text_color"
            android:ellipsize="end"
            android:id="@+id/refresh_label"
            android:fadingEdge="horizontal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/clr_strings_refresh_animation_txt"
            android:layoutDirection="locale"/>
    </LinearLayout>
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/divider_horizontal_bright"
        android:scaleType="fitXY"/>
</LinearLayout>
